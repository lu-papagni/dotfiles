# Dotfiles

> [!IMPORTANT]
> On Unix systems make sure these dependencies are installed:
> - curl
> - tar
> - gzip

Quick install:

```bash
sh -c "$(curl -fsLS get.chezmoi.io/lb)" -- init --apply 'lu-papagni'

```
